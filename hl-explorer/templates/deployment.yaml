apiVersion: apps/v1beta2
kind: Deployment
metadata:
  name: {{ include "hl-explorer.fullname" . }}
  labels:
{{ include "labels.standard" . | indent 4 }}
spec:
  replicas: {{ .Values.replicaCount }}
  selector:
    matchLabels:
      app: {{ include "hl-explorer.name" . }}
      release: {{ .Release.Name }}
  template:
    metadata:
      labels:
        app: {{ include "hl-explorer.name" . }}
        release: {{ .Release.Name }}
    spec:
      volumes:
        {{- if .Values.secrets.adminCert }}
        - name: admin-cert
          secret:
            secretName: {{ .Values.secrets.adminCert }}
        {{- end }}
        {{- if .Values.secrets.adminKey }}
        - name: admin-key
          secret:
            secretName: {{ .Values.secrets.adminKey }}
        {{- end }}
        {{- if .Values.secrets.hlbConnection }}
        - name: hlb-connection
          configMap:
            name: {{ .Values.secrets.hlbConnection }}
        {{- end }}
      containers:
        - name: {{ .Chart.Name }}
          image: "{{ .Values.image.repository }}:{{ .Values.image.tag }}"
          imagePullPolicy: {{ .Values.image.pullPolicy }}
          # TODO: Add liveness and readiness probes
          envFrom:
            - configMapRef:
                name: {{ include "hl-explorer.fullname" . }}--db
          env:
            # Database environmental variable
            {{- if .Values.db.chart }}
            - name: DATABASE_PASSWD
              valueFrom:
                secretKeyRef:
                  name: {{ .Values.db.chart | quote }}
                  key: postgres-password
            {{- end }}
          ports:
            - name: http
              containerPort: 8080
              protocol: TCP
          volumeMounts:
            {{- if .Values.secrets.adminCert }}
            - mountPath: /hl_config/admin/signcerts
              name: admin-cert
            {{- end }}
            {{- if .Values.secrets.adminKey }}
            - mountPath: /hl_config/admin/keystore
              name: admin-key
            {{- end }}
            {{- if .Values.secrets.hlbConnection }}
            - mountPath: /opt/explorer/app/platform/fabric/config.json
              subPath: config.json
              name: hlb-connection
            {{- end }}
          resources:
{{ toYaml .Values.resources | indent 12 }}
    {{- with .Values.nodeSelector }}
      nodeSelector:
{{ toYaml . | indent 8 }}
    {{- end }}
    {{- with .Values.affinity }}
      affinity:
{{ toYaml . | indent 8 }}
    {{- end }}
    {{- with .Values.tolerations }}
      tolerations:
{{ toYaml . | indent 8 }}
    {{- end }}
